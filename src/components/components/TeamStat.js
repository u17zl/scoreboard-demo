import React, { Fragment } from "react";
import "./teamStat.css";
const style = {};

const TeamStat = props => {
  const { homeColor, awayColor } = props;

  return (
    <Fragment>
      <div className="team-stat">Kensington City</div>
    </Fragment>
  );
};

export default TeamStat;
